<%= form_with(model: message) do |form| %>
  <% if message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
        <% message.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :room_id, value: @room.id %>

  <div class="input-group position-absolute bottom-0 start-50 translate-middle-x mb-3">
    <%= form.text_field :content, class:"form-control", placeholder:"Enter a message", autocomplete: "off" %>
    <%= form.submit "Send message", class: "btn btn-success", id: "inputGroup-sizing-default"%>
  </div>
<% end %>
