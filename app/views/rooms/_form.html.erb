<%= form_with(model: room) do |form| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
        <% room.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>  
  <h3>Add room</h3>
  <div class="form-floating mb-3 flex col-7">     
    <%= form.text_field :name, class:"form-control overflow-auto", placeholder:"Enter a room name", id:"floatingTextarea" %>
    <%= form.label :Room_name %>
  </div>
  <div class="mb-3">
    <%= form.submit "Add room", class:"btn btn-primary " %>
  </div>  
<% end %>